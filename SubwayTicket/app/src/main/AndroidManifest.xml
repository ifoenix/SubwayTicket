<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.subwayticket">
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 这个权限用于进行网络定位-->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"></uses-permission>
    <!-- 这个权限用于访问GPS定位-->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"></uses-permission>
    <!-- 用于访问wifi网络信息，wifi信息会用于进行网络定位-->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"></uses-permission>
    <!-- 获取运营商信息，用于支持提供运营商信息相关的接口-->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    <!-- 这个权限用于获取wifi的获取权限，wifi信息会用来进行网络定位-->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"></uses-permission>
    <!-- 用于读取手机当前的状态-->
    <uses-permission android:name="android.permission.READ_PHONE_STATE"></uses-permission>
    <!-- 写入扩展存储，向扩展卡写入数据，用于写入离线定位数据-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>
    <!-- 访问网络，网络定位需要上网-->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- SD卡读取权限，用户写入离线定位数据-->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"></uses-permission>



    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service android:name="com.baidu.location.f" android:enabled="true" android:process=":remote">
        </service>
        <meta-data
            android:name="com.baidu.lbsapi.API_KEY"
            android:value="uvHkvBaEgFOvEse2wVUPVzNNeuNrlqhF" />

        <activity
            android:name=".MainActivity"

             >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.WelcomeActivity">

        </activity>

        <activity android:name=".view.LoginActivity"></activity>
        <activity android:name=".view.RegisterActivity"></activity>
        <activity android:name=".view.FindPasswordActivity"></activity>
        <activity android:name=".view.OrderDetailActivity"></activity>
        <activity android:name=".view.OperGuideActivity"></activity>
        <activity android:name=".view.OrderActivity">

        </activity>
        <activity android:name=".view.PersonalInfoActivity"></activity>
        <activity android:name=".view.SettingsActivity"></activity>
        <activity android:name=".view.SiteActivity"></activity>
        <activity android:name=".view.BuyTicketActivity"></activity>
        <activity android:name=".view.CheckTicketActivity"></activity>
        <activity android:name=".view.LegalActivity"></activity>
        <activity android:name=".view.SafetyActivity"></activity>
        <activity android:name=".view.BusInfomationActivity"></activity>
        <activity android:name=".view.NoticeDetailActivity"></activity>

        <activity android:name=".view.GuideActivity"></activity>



    </application>

</manifest>